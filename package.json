{
  "name": "admin-main",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"No build step required (static frontend)\"",
    "vercel-build": "echo \"vercel build placeholder\""
  },
  "engines": {
    "node": ">=18"
  }
}
